name: Disquests
version: '${version}'
main: com.disqt.disquests.paper.DisquestsPlugin
api-version: '1.21'
commands:
  disquests:
    description: Disquests admin commands
    permission: disquests.admin
    usage: /disquests <reload|reset>
permissions:
  disquests.admin:
    description: Admin access to Disquests commands
    default: op
